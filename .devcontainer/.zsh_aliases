alias ll='ls -al'

# Claude Code isolated container
alias dev-up='docker compose -f docker-compose.dev.yml up -d --build'
alias dev-down='docker compose -f docker-compose.dev.yml down'
alias claude-run='docker exec -it claude-code bash -lc claude'
alias dev-shell='docker exec -it claude-code bash'
